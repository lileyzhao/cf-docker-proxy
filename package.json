{
  "name": "cf-docker-proxy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "pnpm wrangler deploy",
    "dev": "pnpm wrangler dev --env dev",
    "dev:staging": "pnpm wrangler dev --env staging",
    "dev:production": "pnpm wrangler dev --env production",
    "start": "pnpm wrangler dev",
    "test": "pnpm vitest",
    "cf-typegen": "pnpm wrangler types",
    "format": "pnpm dlx prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.48",
    "typescript": "^5.8.3",
    "vitest": "~3.2.4",
    "wrangler": "^4.22.0"
  }
}
